import { ChangeCocolrDirective } from './change-cocolr.directive';

describe('ChangeCocolrDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeCocolrDirective();
    expect(directive).toBeTruthy();
  });
});
